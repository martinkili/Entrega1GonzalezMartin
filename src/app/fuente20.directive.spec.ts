import { Fuente20Directive } from './fuente20.directive';

describe('Fuente20Directive', () => {
  it('should create an instance', () => {
    const directive = new Fuente20Directive();
    expect(directive).toBeTruthy();
  });
});
